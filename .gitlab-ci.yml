image: python:3.12

stages:
  - CheckDependenciesInstallation
  - CheckCodeStyle

check_dependencies:
  stage: CheckDependenciesInstallation
  image: python:3.12
  before_script:
    - pip install --upgrade pip
  script:
    - pip install -r requirements/dev.txt
    - pip install -r requirements/test.txt
  only:
    - main
    - merge_requests


flake8:
  stage: CheckCodeStyle
  image: registry.gitlab.com/pipeline-components/flake8:latest
  script:
    - flake8 --max-line-length=88 --ignore=Q000,Q001,Q002,Q003 .
  only:
    - main
    - merge_requests


Black:
  stage: CheckCodeStyle
  image: registry.gitlab.com/pipeline-components/black:latest
  script:
    - black --check .
  only:
    - main
    - merge_requests
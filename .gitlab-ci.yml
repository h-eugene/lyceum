stages:
  - lint

flake8:
  stage: lint
  image: python:3.12
  before_script:
    - pip install --upgrade pip
    - pip install -r requirements.txt
  script:
    - flake8 .
  only:
    - main
    - merge_requests
